#102：层序遍历即可，加入队列的节点要带有深度信息，且每次深度加深时，都要创建个新的列表加入原有列表中。因此要设置一个pre_d变量记录之前深度。注意输入为空时直接返回空列表。
#107：同上，把链表元素倒序即可

#107官方：我是每次深度+1就创建新列表，而官方解法是一次在每一层创建新列表，然后把这一层(队列里所有节点)所有节点一次处理完（弹出节点，往列表加入弹出节点的值，扩展弹出的节点加入队列），把
#填入这一层值得到的新列表append进外层列表，若是102则到此结束，107需要返回[::-1]

#429：N 叉树的层序遍历，类似的https://leetcode-cn.com/problems/n-ary-tree-level-order-traversal/
