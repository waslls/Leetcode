#https://leetcode-cn.com/problems/add-two-numbers/
#思路：由于倒序存储在链表，故遍历链表，依次相加即可，注意进位。中间会出现两表长度不一致
#代码：
  细节：指针移向下一个节点时，若检测到next为空，将其next指向ListNode(0)（0不影响加法）。进位操作则比较简单。
  整体：最外层while(1)循环，停止条件为两表下一个节点都是none（因为在为短链表不停创建值为0的节点）。最后注意若进位符为1，则要在最高位再添加1
 #note：创建节点时要注意建立和之前节点的连接
