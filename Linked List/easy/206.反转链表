#解法1：(b站gshefenhehn)创建一个新链表，每遍历一个旧链表的值（依次遍历），就把这个旧链表节点插入新链最前面（头部）https://leetcode-cn.com/problems/reverse-linked-list/submissions/提交1.2

#解法2：(官方)迭代法，遍历链表时，将当前节点的next指向前一个节点，同时当前节点的下一个节点会丢失。因此既需要存储当前节点的前一个节点也要保存当前节点的下一个节点，见提交3.4

#解法3：递归方法 还没看
