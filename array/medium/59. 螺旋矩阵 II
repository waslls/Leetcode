#详细思路见代码，主要的思想就是在位置不越界和当前位置没被填充时按行列进行填充，关键是位置索引增加的方向要变，行+1，列+1，行-1，列-1，行+1......
#while(num<=n**2)里先对行填充再对列填充，填充后更新填充的数据和位置索引，填充的终止条件为位置越界或当前位置已经被填充。用两个while循环实现。注意1：两个while
#循环里的位置恢复和更新，2：位置索引的更新方向在两个while结束后要变一次，因为完成一次行列的填充后，方向要变（先右再下对x,y都是+1，先左再上对x,y都是-1）。
