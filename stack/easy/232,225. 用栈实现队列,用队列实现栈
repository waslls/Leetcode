#232 goodnotes力扣-》力扣记录需要注意的东西，即python中stack用[]表示，操作为append和pop，队列用deque表示，操作为append和popleft()
#使用两个栈，一个输出栈一个中转栈，都用列表，加元素就直接append，但是取元素需要用pop，即取list[-1]。主要难点在于push操作，不能直接把x append到输出栈中，因为这样对输出栈使用pop就达成后入
#先出效果了，队列需要后入后出。因此需先把输出栈元素放入临时栈再把x appned进临时栈 然后再把临时栈放入输出栈即可


#225
#先把输出队列的放入临时队列，然后把x append进输出队列 最后把临时队列放入输出队列
